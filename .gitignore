# バックアップファイル（本番DB情報を含む可能性があるためGit管理外）
backups/
*.dump
*.backup
*.sql.zip

# ローカル環境変数（接続文字列・パスワードを含む）
.env.backup.local

# 環境変数全般（接続文字列・トークンを含む。example は明示的に追跡する）
.env
.env.*
!.env.example
!.env.backup.local.example

# Supabase CLI ローカル生成物
supabase/.temp/
supabase/.branches/

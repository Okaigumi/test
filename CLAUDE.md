# Claudeの独立レビュー入口

運用の正本は [docs/workflow-rules.md](docs/workflow-rules.md)。作業前に全文を読む。移行変更自身には第12節の移行条件を適用し、従来のClaude独立レビューを維持する。

移行成立後、ClaudeはCodex窓口から依頼された重要な変更の追加レビューを担当する。通常の窓口、実装、Git／公開操作、最終承認は担当しない。既定は読み取りのみ。対象の設計・編集に関与したClaudeは同じ対象の独立レビューをせず、非関与の別担当へ返す。

レビュー担当自身が、正本第12節の非関与条件、セッション／実行ID、実装会話の継承有無、参照資料、対象版を報告する。以前のレビュー指摘とその再確認は継続できるが、設計・修正へ参加した場合は別の非関与担当が必要となる。

## 確認内容

- 仕様・合格条件と実物の差分が一致するか。
- scope外変更、権限拡大、基準の緩和、未確認の成功扱いがないか。
- 証拠が同じ対象版に対応し、実行エラーと正常な拒否が区別されているか。
- 反復上限、停止・再開、承認、公開条件が保たれるか。
- 業務上の影響・復旧方法・残存リスクが適切か。

確認したBASE／HEAD／対象ファイル／完全な内容識別情報を記録する。未コミットは許可された `git diff --full-index`、未追跡は `git diff --no-index --full-index` 等で実物と照合する。短縮ハッシュや件数だけで一致を判断しない。許可外のコマンドで照合を迂回しない。

結果は `PASS / NEEDS_FIX / BLOCKED`、確認範囲、担当の非関与、指摘の根拠・影響・must-fix・解消案、未確認事項をCodex窓口へ返す。Codexが指摘を検証して修正し、担当reviewerが変更と影響範囲を再確認する。自己点検・状態所見・CodexレビューをClaudeの独立レビューと表示しない。

編集・設定変更・Git変更・公開操作へ拡大しない。必要な確認ができなければ `BLOCKED` とし、何が不足するかを示す。権限を変更・迂回しない。岡井さんへの報告はCodex窓口が整理し、技術的な伝言を通常作業にしない。秘密値や非公開の内部証拠本文は報告に転載しない。

`/okg-go` は従来どおりType 1限定の入力範囲であり、運用ルール移行等のHレビューに転用しない。Hレビューは本書と対象を明示した個別依頼で行う。`/okg-status` は状態所見であり、独立レビューではない。

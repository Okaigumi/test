# SEC-5D5-001：従業員login RPC hash-only化 設計

記録開始：2026-09-21 19:30 JST。正本：[workflow-rules.md](../workflow-rules.md)。前提確認：[SEC-5D5-PREFLIGHT-001](SEC-5D5-PREFLIGHT-001.md)。

## 識別・状態

- 作業ID：`SEC-5D5-001`
- 更新日時・担当：2026-09-21 20:01 JST、Codex task `01a0bc3a-7ee9-7f70-82d9-b650ce736753`
- 状態：DONE（設計工程のみ。SQL案・ローカル静的検証・非関与Claude H再レビューPASS。実DB・Git・公開は未実施）
- 今回の終点：Phase 5-D-5のhash-only化SQL案を固定し、ローカル検証と非関与Claude Hレビューを完了する。実DB適用・Git・公開は含めない。
- BASE／HEAD：`b227e3f09a29a1424cbb04c47a54280c7e569475`。branch `codex/sec-5d5-readonly-preflight` の隔離worktreeを継続利用。
- 対象：本記録、`docs/sql/phase5d-5-employee-login-hash-only.sql`。前作業の未追跡記録を保全する。
- 保全対象：元worktreeの既存hook 2件、前作業記録、既存SQL・アプリ・DB・Supabase・Git履歴・公開状態。

## 目的・範囲・判断

- 業務目的：全従業員のhash移行完了後、login RPCから平文PIN fallbackを除去し、平文列が残る間も認証には使用しない状態を設計する。
- 変更範囲：`create_employee_session(uuid,text)` のstep (6)だけをdual-readからhash-onlyへ変更するSQL案と、安全guard／post-check／smoke計画。
- 対象外：`employees.pin`列DROP（5-D-6）、create／update RPC、管理者PIN、frontend変更、実DB実行、Git、PR、Preview、公開。
- 採用方針：DROPせずCREATE OR REPLACEを使い、現行fingerprint、全件hash整合、権限をfail-closed guardで固定する。signature、引数名、RETURNS、throttle、session発行を維持する。
- 既知の制約：新定義のPostgreSQL canonical fingerprintは実DB未実行のため未確定。BODY後の構造・属性・権限をtransaction内で検証し、POST-COMMITでlength／md5を安全に取得する。rollback案は新fingerprintに依存せず構造と属性をguardし、コメント解除後の版を別途Hレビュー・承認するまで実行しない。
- レビュー区分：H（認証・DB・SQL）。非関与Claudeレビュー必須。

## 合格条件

| ID | 条件 | 検証 | 状態 |
|---|---|---|---|
| AC-01 | 現行RPCがlength=3798／md5=`006550c3455e34aa9d1d61bd60bb85ad`で全件hash前提を満たす時だけBODYへ進む | PRE-CHECKとtransaction guard | 自己点検PASS（実DBではNOT_RUN） |
| AC-02 | step (6)から平文`e.pin = pin_input`とdual-read CASEを除去し、hash-only条件にする | 完全差分・post-check | 自己点検PASS |
| AC-03 | signature・引数名・RETURNS・owner・SECURITY DEFINER・search_path・volatilityを維持する | catalog post-check | 自己点検PASS |
| AC-04 | throttle・session発行の必須markerを維持する | normalized `prosrc` marker検査 | 自己点検PASS |
| AC-05 | pin／pin_hash直接権限0、必要なRPC EXECUTE 6、login RPCのACL全文を実行前後で維持する | pre/post guard | 自己点検PASS（PUBLIC値は未観測・PRE-CHECKで情報表示） |
| AC-06 | 全件hash整合とcost 12をpre/postで維持し、秘密値を出さない | 集計だけを確認 | 自己点検PASS |
| AC-07 | SQLが構文・禁止操作・範囲のローカル検証に合格する | static scan／差分照合 | 構造・差分PASS、PostgreSQL parser NOT_RUN |
| AC-08 | 非関与Claude HレビューPASS／must-fix 0 | 固定blobレビュー | PASS（session `19a21b45-f477-4bd4-8b44-79bcdbd92124`） |

- 固定条件の出所：`SEC-5D5-PREFLIGHT-001`のPASS、岡井さんのPhase 5-D-5設計着手承認と「進めて」。
- 条件変更：なし。変更する場合は正本第12節に従う。

## 承認

| 操作 | 出所・範囲 | 状態 |
|---|---|---|
| 新作業ID、SQL案、ローカル検証、Hレビュー | Codexが「新しい作業IDでSQL案作成・ローカル検証・Hレビューまで」と限定提示後、岡井さんの「進めて」 | 承認済み |
| 非関与Claudeへの限定外部送信 | SQL案、当作業記録、preflight記録、`CLAUDE.md`、workflow-rules、roadmap、db-migrations、rls-security-plan、5-D-4 closeout、5-D-1〜3 SQL、`index.html`の計13ファイルのみ。秘密値・live DBデータは含めない旨を提示後、岡井さんの「承認します」 | 承認済み |
| 実DB／SQL実行 | 岡井さん限定。対象版・実行手順の別承認が必要 | 未承認 |
| stage／commit／push／PR／Preview | 対象固定後の別承認が必要 | 未承認 |
| merge／本番公開 | 別の最終承認が必要 | 未承認 |

## 実行枠・反復

- 編集担当：Codex窓口1名。対象2ファイルだけを編集する。
- 前作業：`SEC-5D5-PREFLIGHT-001`はDONE。目的・対象が新規のため修正巡数は引き継がない。
- 当該IDの使用済み修正巡数：2／上限2。第1巡は初版自己点検で、step (6)以外のコメント復元、post-checkの0行fail-open解消、PUBLIC EXECUTE観測、create／update dual-write fingerprint guardを補強。第2巡は第1回HレビューのM-1／M-2を受け、事前固定したコメント化rollback案とACL前後同一性guardへ修正。
- 同一失敗・新情報なし：0／上限2。
- 時間枠：2026-09-21 19:30〜20:00 JSTの30分。終了後は新規操作を開始しない。
- 自動運転：未使用。実DB・外部変更は行わない。

## レビュー

- 必要観点：hash-only性、現行不変条件、guardのfail-closed性、権限、rollback、秘密値非出力、5-D-6への越境防止。
- 第1回非関与Claude担当：`claude-sonnet-5`、session `c6208dc7-435c-48ce-bb2b-828f92db88ef`。実装・修正に非関与の新規context。
- 第1回固定blob：SQL `66ea607b739657693a7e4fce349a0a6d4b074372`、記録 `2391fbb63b9039fc191fef4c8eba6f18088a07dd`、保全対象 `c4e4e702a4c08d11c7ed814cbda4576af00a109e`。レビュー前後一致。
- 第1回結果：NEEDS_FIX（M-1 rollback実体・参照先不足、M-2 PUBLIC EXECUTE=0を未観測のままPASS扱い）。助言はPRE-CHECK 0行表示、signature表示、smoke対象、記録更新、parser NOT_RUN等。
- 修正：同ファイル内へコメント化したdual-read rollback案・構造／属性／writer baseline／ACL同一性guardを追加。PUBLIC値は情報表示に下げ、BODYは実行前ACL全文をtransaction-local設定へ保存してpost-checkで同一性を比較する。
- 再レビュー：別の非関与Claude `claude-sonnet-5`、session `19a21b45-f477-4bd4-8b44-79bcdbd92124`。SQL `420c907c12ed77ddcb5ec632c9df83f21ed0f589`、記録 `ec6fc92fa84a30a6c600e71c6d7b0dc4f62b7425`、保全対象 `c4e4e702a4c08d11c7ed814cbda4576af00a109e`を前後一致で確認。PASS、must-fix 0。
- 再レビュー助言：実行時はPart 2のBEGIN〜COMMITを必ず一括実行する。rollback案はコメント解除前にpin列存在・marker・復元fingerprintを補強して再レビューする。PRE-CHECKの関数不在時0行表示は安全側だが見落としに注意する。いずれも今回のmust-fixではなく、SQLは追加修正しない。

## ローカル検証

- `git diff --check`：PASS。
- 第2巡修正後SQL blob：`420c907c12ed77ddcb5ec632c9df83f21ed0f589`。作業記録は本追記後に再固定する。
- 現行5-D-1関数と新関数をstep (6)だけplaceholder化して比較：`equal_except_step6=True`。現行block 3741文字、新block 3587文字。
- transaction構造：BEGIN 1、COMMIT 1、DO open/close各2、CREATE OR REPLACE FUNCTION 1。
- BODYの実行文にDROP／ALTER／TRUNCATE／GRANT／REVOKE／employeesデータ更新はない。Part 4 rollback案は全体をコメント化し、明示的に実行不可としている。
- hash-only条件、key-share、throttle、session delete/insertのmarkerを確認。
- psqlとSQL parser packageは利用不可。Docker CLIは存在するがdaemon停止のため構文検証はERROR。daemon起動・image取得は未承認なので行わない。PostgreSQL parserによる構文検証はNOT_RUNとし、静的確認とHレビューで補うが、実DB検証済みとは扱わない。
- SQL／DB実行：NOT_RUN。

## 再開・完了

- 現在動作中の処理：なし。
- 次に許可された操作：なし。実DB適用へ進むには、固定SQL blob、PRE-CHECK、Part 2一括実行手順、POST-COMMIT／smoke、停止条件を提示した別の明示承認が必要。
- 待機事項：実DB・Gitは未承認。修正巡数は上限到達。
- 実装：SQL案のみ。DB未適用。
- Git／公開：未実施。
- 今回の終点：達成（設計工程のAC-01〜08。実DBでのAC-01〜07再確認は未実施であり、DB適用完了を意味しない）。
- 後継記録：Production適用・改訂完了条件・現行DB状態は`SEC-5D5-EXEC-001`を参照する。本節のDB未適用表示は設計工程完了時点の履歴である。

## 操作記録

| ID | 操作 | 状態 | 証拠 |
|---|---|---|---|
| OP-01 | 新規SQL・作業記録の非関与Claude Hレビュー起動 | 未実施・安全審査拒否 | process開始前に拒否。session未成立、外部送信なし |
| OP-02 | 限定13ファイルの非関与Claude Hレビュー | NEEDS_FIX | session `c6208dc7-435c-48ce-bb2b-828f92db88ef`、M-1／M-2 |
| OP-03 | M-1／M-2の最終修正巡 | 完了 | rollback案固定、ACLを実行前後同一性へ変更 |
| OP-04 | 別の非関与Claudeによる修正範囲H再レビュー | PASS | session `19a21b45-f477-4bd4-8b44-79bcdbd92124`、must-fix 0、SQL blob前後一致 |

## 決定履歴

| 日時 | 決定 | 根拠 | 次の対応 |
|---|---|---|---|
| 2026-09-21 19:30 JST | 5-D-5設計を新作業IDで開始 | 岡井さんの「承認します」「進めて」 | SQL案と記録を作成し、実DB・Gitへ進まない |
| 2026-09-21 19:36 JST | Hレビュー起動を停止 | 新規SQL・作業記録の外部送信範囲が具体的に未承認 | 外部送信なし。対象限定承認待ち |
| 2026-09-21 19:39 JST | 限定13ファイルの外部送信承認を受領 | 対象一覧と目的を明示後、岡井さんの「承認します」 | 再固定して非関与Claude Hレビューを起動 |
| 2026-09-21 19:52 JST | HレビューM-1／M-2を第2巡で修正 | 復旧可能性と未観測PUBLIC前提の解消が必要 | 別の非関与Claudeで修正範囲を再レビュー |
| 2026-09-21 20:01 JST | 設計工程をDONEとする | H再レビューPASS／must-fix 0。結果待ちは19:52に開始済みで、20:00以降は新規修正を開始せず結果記録のみ | DB・Git・公開へ進まず停止 |
